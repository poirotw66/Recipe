---
import ScenariosListPage from "../../../components/ScenariosListPage.astro";
import BaseLayout from "../../../layouts/BaseLayout.astro";
import { localePath } from "../../../lib/i18n";
import { getScenariosListPage } from "../../../i18n/scenarios-list-page";
import { localeStaticPaths, requireNonDefaultLocale } from "../../../lib/i18n-routes";
import { listRecipesForLocale } from "../../../lib/recipe-locale";
import { sortRecipesByPublishedDate } from "../../../lib/recipes";

export function getStaticPaths() {
  return localeStaticPaths();
}

const locale = requireNonDefaultLocale(Astro.params.locale);
const content = getScenariosListPage(locale);
const recipes = sortRecipesByPublishedDate(await listRecipesForLocale(locale));
const canonical = localePath(locale, "/scenarios");
---

<BaseLayout
  title={content.title}
  description={content.description}
  canonicalPath={canonical}
  alternatePath="/scenarios"
  locale={locale}
>
  <ScenariosListPage locale={locale} recipes={recipes} />
</BaseLayout>
